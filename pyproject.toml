[project]
name = "password-manager"
version = "0.1.0"
description = ""
authors = [
    {name = "Alberto Barrago", email = "albertobarrago@gmail.com"}
]
requires-python = ">=3.8"
dependencies = [
    "pyperclip>=1.8.0",
    "python-dotenv",
    "cryptography",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false
